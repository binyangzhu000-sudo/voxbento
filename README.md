Screenshots for fossasia/voxbento#468 (Atlas Cloud Seed ASR 2.0 transcription provider).

`5-listener-live-captions-atlascloud.png` is the listener page rendering a caption
produced by the Atlas Cloud provider. `listener-websocket-frames.json` is the raw
frame log captured from the page's /ws/captions websocket during that run.
